import enums.MeasureType;

import enums.ProductCategory;

public class Sugar extends Product {

    public Sugar(String name, double cost, MeasureType measureType, ProductCategory productCategory) {
        super(name, cost, measureType, productCategory);
    }
}
